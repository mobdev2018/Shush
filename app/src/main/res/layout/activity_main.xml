<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.knafayim.shush.MainActivity">

    <android.support.v7.widget.SwitchCompat
        android:id="@+id/service_switch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@+id/yananeska.ttf"
        android:textStyle="bold|italic"
        android:textSize="18sp"
        android:theme="@style/SwitchCompatTheme"
        android:text="@string/start_geofence"/>

    <Button
        android:text="Locations"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:id="@+id/locationButton"
        android:background="@drawable/rounded_button"
        android:textColor="@android:color/white"
        android:layout_weight="1"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:onClick="goToLocationActivity" />

    <Button
        android:text="Settings"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:id="@+id/volumeSettingsButton"
        android:background="@drawable/rounded_button"
        android:textColor="@android:color/white"
        android:layout_weight="1"
        android:layout_below="@+id/locationButton"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:onClick="goToVolumeSettings" />

    <Button
        android:text="About"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:id="@+id/aboutButton"
        android:background="@drawable/rounded_button"
        android:textColor="@android:color/white"
        android:layout_weight="1"
        android:layout_marginTop="80dp"
        android:layout_below="@+id/volumeSettingsButton"
        android:layout_centerHorizontal="true"
        android:onClick="goToAbout" />

    <TextView
        android:layout_alignParentBottom="true"
        android:text="To remove Shush notifications in your notifications panel, please go to Settings > Notifications and disable notifications for this app"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:fontFamily="@+id/yananeska.ttf"
        android:textColor="@android:color/darker_gray"
        />

</RelativeLayout>
